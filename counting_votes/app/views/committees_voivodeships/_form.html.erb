<%= form_for(@committees_voivodeship) do |f| %>
  <% if @committees_voivodeship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@committees_voivodeship.errors.count, "error") %> prohibited this committees_voivodeship from being saved:</h2>

      <ul>
      <% @committees_voivodeship.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :committee_id %><br>
    <%= f.text_field :committee_id %>
  </div>
  <div class="field">
    <%= f.label :voivodeship_id %><br>
    <%= f.text_field :voivodeship_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
